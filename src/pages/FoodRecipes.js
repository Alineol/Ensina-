import React from 'react';
// import context from '../context/context';

function RecipesScreen() {
  // const { renderFoodRecipes } = useContext(context);

  return (
    <p>pagina de receitas</p>
  );
}

export default RecipesScreen;
